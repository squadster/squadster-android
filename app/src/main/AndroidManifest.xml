<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.squadster">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.android.squadster.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_with_background"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_with_background_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Splash"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.android.squadster.app.AppActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>